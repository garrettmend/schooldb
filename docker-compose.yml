version: '3.4'
services:
  lms:
    build:
      context: ./LMS
      dockerfile: Dockerfile
    image: garrettmend/schooldb:local
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - MYSQL_URL=mysql://root:DfGxDgpWqieOMDuizbkDgFDIaHAJycGq@mysql.railway.internal:3306/railway
      - DATA_PROTECTION_PATH=/var/keys
    volumes:
      - dp_keys:/var/keys
    ports:
      - "5000:80"

volumes:
  dp_keys:
